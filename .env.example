# All of these are read server-side only, by server/index.js (which starts
# the connectors in server/connectors/ -- see server/scheduler.js). None are
# ever exposed to the browser bundle -- that's why none are VITE_-prefixed.
#
# Not listed below because they need no key at all: Feodo Tracker, OpenPhish,
# Emerging Threats (Proofpoint), Spamhaus DROP, and the CVE Program's own
# cve.org delta feed -- all four are public plain-text/JSON feeds with no
# registration.

# --- Bulk/background sources -------------------------------------------

# Optional. Free key from https://nvd.nist.gov/developers/request-an-api-key
# Raises the NVD CVE API rate limit from 5 requests/30s to 50 requests/30s.
NVD_API_KEY=

# Recommended. Free key from https://auth.abuse.ch/, shared across abuse.ch's
# ThreatFox, URLHaus and MalwareBazaar APIs (they unified auth in 2023-2024 --
# URLHaus used to be keyless but no longer is, confirmed against the live
# API). Without it, those three panels show a "not configured" message and
# the Threat Feed falls back to Feodo Tracker + OpenPhish (both keyless).
ABUSECH_AUTH_KEY=

# Optional. Free key from https://otx.alienvault.com/ (Settings -> API
# Integration). Powers OTX pulse IOCs in the Threat Feed and the OTX-tagged
# half of Threat Actor Activity. Without it, that connector reports
# "not configured" and is skipped.
OTX_API_KEY=

# Optional. Free key from https://www.abuseipdb.com/register (free tier: a
# daily quota of lookups + a bulk /blacklist endpoint). Powers "Top Malicious
# IPs" via the Threat Feed. Without it, this source is skipped.
ABUSEIPDB_API_KEY=

# Optional. Free key from https://pulsedive.com/ -- confirmed live that
# Pulsedive's explore/info endpoints actually work without a key at all, but
# an anonymous IP shares a much lower rate limit than a registered one. Powers
# a "risk=high" slice of the Threat Feed and doubles as an IOC Search lookup
# (ip/domain/url; no file-hash support).
PULSEDIVE_API_KEY=

# Required for the bulk feed. Free key from https://phishtank.org/ (register,
# then find it under your profile). Confirmed live: anonymous downloads are
# now capped at 75 requests/3 days and routed through a CDN that 404s without
# a key, so this source reports "not configured" until one is set.
PHISHTANK_API_KEY=

# Strongly recommended (works without one, but barely). Free personal access
# token from https://github.com/settings/tokens -- no scopes needed for
# public data. Confirmed live: raises GitHub's Search API limit from 10 to 30
# req/min, and the REST ("core") limit from 60 to 5000 req/hr. Powers
# server/githubIntel/ (repo discovery, content fetch, IOC/CVE/ATT&CK
# extraction, correlation, threat scoring), and the YARA-Rules/SigmaHQ
# detection-rule sync (server/connectors/detectionRules.js), which reuses the
# same GitHub client. Without it, discovery/enrichment still run, just much
# slower and more likely to hit 429s mid-cycle.
GITHUB_TOKEN=

# Optional. Free Community API key from https://vulncheck.com/ (Community
# tier -- registration required, no cost). Powers a second, larger exploited-
# CVE catalog (VulnCheck KEV, confirmed live at api.vulncheck.com/v3/index/
# vulncheck-kev) alongside CISA's own KEV -- 2,500+ tracked CVEs with linked
# exploit references, vs CISA's ~1,300. Without it, this source reports
# "not configured" and is skipped, same pattern as OTX/AbuseIPDB/etc.
VULNCHECK_API_KEY=

# --- Keyless bulk/background sources (no registration, nothing to set) --
#
# Exploit-DB (server/connectors/exploitdb.js) -- free CSV mirror of Exploit-DB,
# CVE<->PoC-maturity correlation, no auth.
# YARA-Rules + SigmaHQ (server/connectors/detectionRules.js) -- free,
# community-maintained detection-rule repos, filename-based family index.

# --- Intelligence Investigation Console only (single-indicator lookups, ---
# --- not bulk feeds -- formerly "IOC Search") ------------------------------

# Optional. Free key from https://www.virustotal.com/gui/join-us (free tier:
# 4 req/min, 500/day). Used live when you search an indicator in IOC Search --
# never polled/cached in bulk.
VIRUSTOTAL_API_KEY=

# Optional. Free Community API key from https://viz.greynoise.io/ -- IP
# lookups only. Used live in IOC Search.
GREYNOISE_API_KEY=

# Optional. Free key from https://account.shodan.io/register -- IP lookups
# only, the most limited free tier of the five. Used live in IOC Search.
SHODAN_API_KEY=

# Optional. Free key from https://hybrid-analysis.com/ (Falcon Sandbox
# self-service account -> Profile -> API Key). Confirmed live that the old
# bulk "recent samples" feed no longer exists (404); only the hash-lookup
# endpoint is still live, so this is file-hash lookups only in IOC Search.
HYBRID_ANALYSIS_API_KEY=

# Optional. Free key from https://leakix.net/ (register -> account -> API
# key). IP/domain lookups only -- exposed-service and leaked-data OSINT
# search, confirmed live via /host/{ip} and /search?scope=service|leak.
LEAKIX_API_KEY=

# Optional. Free key from https://urlscan.io/user/signup (small free
# submission allowance). URL lookups only, in the Intelligence Investigation
# Console's URL module -- provides a real screenshot, redirect chain,
# rendered page title, and classification verdict. Searching URLs already
# scanned by someone else works with NO key at all; this key is only needed
# to submit a fresh scan for a URL nobody has scanned yet.
URLSCAN_API_KEY=

# The Intelligence Investigation Console's Domain module (WHOIS via RDAP,
# DNS records, SPF/DKIM/DMARC, passive DNS via the existing OTX_API_KEY
# above) and IP module's passive DNS/reverse DNS need NO new keys at all --
# RDAP and DNS resolution are free/keyless protocols, and passive DNS reuses
# the OTX key already configured above.

# The following five IOC Search sources need no key at all (confirmed live,
# keyless, no registration) -- listed here for completeness, not because
# there's anything to set:
#   - crt.sh (server/lookups/crtsh.js) -- domain lookups, certificate
#     transparency log search / subdomain discovery.
#   - RIPEstat (server/lookups/ripestat.js) -- IP lookups, ASN/prefix/holder.
#   - Team Cymru (server/lookups/teamCymru.js) -- IP + hash lookups, via DNS
#     TXT queries (IP-to-ASN and the Malware Hash Registry), not REST.
#   - Hudson Rock Cavalier (server/lookups/hudsonRock.js) -- domain lookups,
#     free infostealer-exposure intelligence.
#   - SANS ISC / DShield (server/lookups/isc.js) -- IP lookups, recent
#     honeypot-reported activity + cross-referenced third-party blocklists.
#     Confirmed live against ISC's own docs: their query API has no API-key
#     mechanism at all ("we do not require authentication"); a registered
#     account's API key on isc.sans.edu is for *submitting* honeypot/firewall
#     logs, a different, write-only feature this dashboard doesn't use.

# --- AI Assistant (local RAG chatbot, see server/rag/) ------------------
# No key, no account, no cost -- these just point at a local Ollama install
# (https://ollama.com/download, free). After installing, pull both models
# once: `ollama pull llama3.1:8b` and `ollama pull nomic-embed-text`. Leave
# these unset to use the defaults shown below; the AI Assistant tab reports
# itself as unavailable (with exact setup instructions) until Ollama is
# running and both models are pulled -- nothing else in the dashboard depends
# on this.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3.1:8b
OLLAMA_EMBED_MODEL=nomic-embed-text

# --- AI Summarization + combined entity extraction ----------------------
# Required for both of these features -- unlike the AI Assistant above,
# neither runs on local Ollama. Free key, no card required:
# https://console.groq.com/keys. Leave unset to disable both -- each reports
# itself unavailable (same pattern as every other optional keyed source
# here) rather than failing loudly.
#
# server/ai/ -- generic, provider-agnostic AIRouter (aiRouter.summarize(prompt) /
# aiRouter.summarizeJson(prompt, opts)), used by every LLM call in this app:
# AI Summarization's 25+ section structured SOC report (server/aiThreatSummary.js,
# this app's single heaviest LLM call), Industry Intelligence briefings
# (server/industryBriefing.js), AI-drafted detection artifacts
# (server/detectionRuleDraft.js), combined entity extraction
# (server/combinedExtraction.js, feeding Malware/Actor/Campaign/Dark Web/Tools
# Intelligence), and the Investigation Workspace's AI narratives (Should I
# Care?, Correlation Summary, AI Investigation Summary/Report).
#
# Tries up to 10 providers in order -- Gemini -> Groq -> NVIDIA NIM ->
# OpenRouter -> Ollama Cloud -> Hugging Face -> Cohere -> Together AI ->
# Cloudflare Workers AI -> GitHub Models -- failing over automatically on
# rate limits/quota/timeouts/5xx/auth errors. Anthropic, Mistral, Cerebras,
# and SambaNova are deliberately not in this chain -- all four are paid/
# billing-gated (confirmed live: SambaNova returns 402 PAYMENT_METHOD_REQUIRED,
# balance 0, on every model) and this deployment doesn't fund them. Every key
# below is optional; an unconfigured provider is skipped, not treated as a
# failure. A provider
# that just failed is put into a temporary cooldown (server/ai/providerHealth.js)
# rather than retried immediately on the next request. See
# server/routes/dashboard.js's /ai-provider-health and /ai-usage routes
# (rendered in the Sources tab) for live per-provider status and token usage.
#
# This is deliberately AI-ASSISTED, not AI-DEPENDENT: every verdict, evidence
# item, and relationship in this platform is computed deterministically from
# real intelligence sources BEFORE any of this is called (see
# server/investigation/evidence.js, verdictEngine.js, investigationGraph.js).
# If every provider below is unavailable, the platform keeps working --
# AllProvidersFailedError is caught everywhere it's thrown and the UI falls
# back to showing the deterministic result with a plain "AI narrative
# unavailable" note instead of an error page.
# Moving all of this off a single local model avoids that model's own
# memory/reliability limits under sustained load, and is what makes AI
# Summarization plus the five Intelligence tabs work on a hosted deploy with
# no Ollama available at all.
# Free tier, no card required: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Optional override -- defaults to gemini-flash-latest (Google's maintained
# alias; a pinned "gemini-2.5-flash" 404s for newer API keys -- confirmed
# live, "no longer available to new users" per Google's own error message).
GEMINI_MODEL=
# Optional override -- defaults to gemini-flash-lite-latest, Google's own
# lighter/faster/higher-quota tier below Flash. Only used when a caller
# requests {tier: "fast"} -- currently just server/combinedExtraction.js
# (see GROQ_EXTRACTION_MODEL below for why).
GEMINI_FAST_MODEL=
GROQ_API_KEY=
# Optional override -- defaults to llama-3.3-70b-versatile (Groq's largest
# free-tier model; AI Summarization's report schema needs a strong model to
# fill out reliably).
GROQ_CHAT_MODEL=
# Optional override -- defaults to openai/gpt-oss-20b. Deliberately a
# smaller/faster model than GROQ_CHAT_MODEL above, used only when a caller
# requests {tier: "fast"}: combined entity extraction is a much lighter
# six-category name/ID extraction, not a 25+ section report, and an
# 8B-class model carries a materially higher free-tier rate limit, which
# matters more here since this job runs continuously against dozens of
# articles per cycle rather than once a day. (llama-3.1-8b-instant, the
# previous default, was decommissioned by Groq -- confirmed live: 404
# model_not_found.)
GROQ_EXTRACTION_MODEL=
# NVIDIA NIM -- free developer tier, sign up at https://build.nvidia.com,
# open any model card and click "Get API Key" (key starts with nvapi-).
NVIDIA_API_KEY=
# Optional override -- defaults to nvidia/llama-3.3-nemotron-super-49b-v1.
# Confirmed live: meta/llama-3.3-70b-instruct and meta/llama-3.1-70b-instruct
# both hang indefinitely (no error, no response, 90s+) on this account --
# this Nemotron model (same Llama-3.3 lineage, 49B) answers normally.
NVIDIA_MODEL=
# Optional override -- defaults to meta/llama-3.1-8b-instruct, confirmed
# working (including JSON mode) for the fast tier.
NVIDIA_FAST_MODEL=
# Free tier, no card required: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=
# Optional override -- defaults to command-r-08-2024 (the undated "command-r"
# was retired -- confirmed live, "removed on September 15, 2025" per
# Cohere's own error message).
COHERE_MODEL=
# Optional override -- defaults to command-r7b-12-2024, Cohere's own
# lightweight tier of the same model family. Fast-tier equivalent of
# GEMINI_FAST_MODEL/MISTRAL_FAST_MODEL above.
COHERE_FAST_MODEL=

# -- Additional AI providers, all optional, same skip-if-unconfigured
# behavior as the two above. Every *_MODEL override below is optional too --
# each defaults to a currently-live free-tier model, but provider catalogs
# shift, so verify against the linked docs if a default ever 404s.
#
# OpenRouter is a MODEL ROUTER, not one fixed model -- OPENROUTER_MODEL picks
# which upstream model it forwards a request to. Browse free models (no card
# required) at https://openrouter.ai/models?max_price=0, then get a key at
# https://openrouter.ai/keys.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
OPENROUTER_FAST_MODEL=
# Monthly free inference credits, no card required: https://huggingface.co/settings/inference-providers
HUGGINGFACE_API_KEY=
HUGGINGFACE_MODEL=
HUGGINGFACE_FAST_MODEL=
# Trial-credit-based, not permanently free like the others above:
# https://api.together.ai/settings/api-keys
TOGETHER_API_KEY=
TOGETHER_MODEL=
TOGETHER_FAST_MODEL=
# Cloudflare Workers AI needs TWO credentials, not just one key: an API
# token (Workers AI edit permission) from
# https://dash.cloudflare.com/profile/api-tokens, plus your account ID
# (visible on any zone's Overview page in the Cloudflare dashboard). Free
# daily neuron budget, no card required.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_MODEL=
CLOUDFLARE_FAST_MODEL=
# Free for any GitHub account (rate limits scale with Copilot subscription
# tier) -- create a fine-grained personal access token with "Models: Read"
# permission at https://github.com/settings/personal-access-tokens.
GITHUB_MODELS_TOKEN=
GITHUB_MODELS_MODEL=
GITHUB_MODELS_FAST_MODEL=
# Ollama Cloud -- free, no credit card, sign up at https://ollama.com then
# generate a key under Settings -> API Keys. Free tier is GPU-time-based
# (session limits reset every 5h, weekly limits every 7 days), not a fixed
# daily request count like the providers above.
OLLAMA_API_KEY=
OLLAMA_MODEL=
OLLAMA_FAST_MODEL=

# Optional: override the AI provider fallback order (comma-separated
# provider keys). Defaults to the full 10-provider chain in the order
# documented above. Unknown keys are ignored; any known provider you omit is
# still appended at the end rather than dropped, so a typo here can't
# silently disable a configured provider. Example:
# AI_PROVIDER_ORDER=gemini,groq,nvidia,cohere,ollama
AI_PROVIDER_ORDER=

# Port the backend (server/index.js) listens on. Vite's dev proxy also reads
# this to know where to forward /api/* -- see vite.config.ts.
PORT=8080
